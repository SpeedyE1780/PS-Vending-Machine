<UserControl x:Class="Project.ComparePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Project"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="600">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="40"/>
            <Setter Property="MaxWidth" Value="600"/>
            <Setter Property="TextWrapping" Value="WrapWithOverflow"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>
    <StackPanel Orientation="Vertical">
        <Image Name="imgGameCover" Width="400" Height="400"/>
        <TextBlock Name="txtGameName"/>
        <TextBlock Name="txtGameCompany"/>
        <TextBlock Name="txtGameDate"/>
        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" Name="panelGameRating" Margin="10"/>
        <TextBlock Name="txtGameAgeRating"/>
        <TextBlock Name="txtGamePrice"/>
    </StackPanel>
</UserControl>
